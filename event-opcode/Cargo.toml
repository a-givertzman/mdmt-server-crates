[package]
name = "event-opcode"
edition.workspace = true
version.workspace = true
authors.workspace = true

[lib]
proc-macro = true

[dependencies]
quote = "~1.0"
syn = { version = "~2.0", features = ["visit"] }

[dev-dependencies]
trybuild = "~1.0"